This is a system of makefiles to build coq and the programs it needs.

Quick start for software builders, do once for the users:

      make -C src

Quick start for software users (with bash or sh as shell):

      . bin/with +
      coqide
      ... or other coq commands ...

To install the software permanently, add this to your .profile:

      . bin/with +
